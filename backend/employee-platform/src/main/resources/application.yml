spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/employee_platform
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
   
jwt:
    secret: 6c808e9d-1bd7-4768-a1e0-ddb09e6cf147
    expiration: 900000

refresh:
  expiration: 2592000000

server:
  port: 8080
